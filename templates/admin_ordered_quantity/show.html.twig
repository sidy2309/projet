{% extends 'base.html.twig' %}

{% block title %}OrderedQuantity{% endblock %}

{% block body %}
    <h1>OrderedQuantity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordered_quantity.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ ordered_quantity.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_ordered_quantity_index') }}">back to list</a>

    <a href="{{ path('admin_ordered_quantity_edit', {'id': ordered_quantity.id}) }}">edit</a>

    {{ include('admin_ordered_quantity/_delete_form.html.twig') }}
{% endblock %}
