<form method="post" action="{{ path('admin_payment_request_delete', {'id': payment_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_request.id) }}">
    <button class="btn">Delete</button>
</form>
